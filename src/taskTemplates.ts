/** 新建任务的场景模板：选中后填入 README 的结构化骨架，用户再按需修改。
 *  针对「指令笼统 → 产出口水稿」的通病：骨架强制写清目标/范围/产出/约束。 */
export interface TaskTemplate {
  label: string;
  content: string;
}

export const TASK_TEMPLATES: TaskTemplate[] = [
  {
    label: "调研报告",
    content: `# 调研报告

## 目标
调研「」并回答：……

## 范围
- 包含：
- 不包含：

## 输出格式
- 产出：report.md（或按需要调整）
- 结构：结论摘要 → 关键发现 → 数据/来源 → 建议
- 篇幅：不超过 2000 字，关键结论前置

## 约束
- 数据需注明来源与时间，不臆造
- 拿不准的信息标注「待核实」
`,
  },
  {
    label: "数据处理",
    content: `# 数据处理

## 目标
把「」处理成「」，用于……

## 范围
- 输入：references/ 下的（文件名/格式/量级）
- 不包含：

## 输出格式
- 产出：output/ 下的（CSV / JSON / Markdown 汇总）
- 字段/结构：

## 约束
- 不改动原始数据文件，只读
- 异常/缺失数据单独列出，不静默丢弃
- 处理脚本留在 scripts/ 下，可重复执行
`,
  },
  {
    label: "日报/周报",
    content: `# 日报/周报

## 目标
汇总「」在（今天/本周）的进展，产出一份简报。

## 范围
- 数据来源：references/ 下的（日志 / 导出的记录 / 链接清单）
- 时间范围：

## 输出格式
- 产出：report.md
- 结构：本期要点（3-5 条）→ 分类明细 → 风险/待办
- 每条一行，事实优先，不写套话

## 约束
- 只基于已有材料，不臆测
- 无数据的部分直接写「本期无」，不硬凑
`,
  },
  {
    label: "代码任务",
    content: `# 代码任务

## 目标
实现/修复「」，解决……

## 范围
- 涉及：（模块 / 文件 / 功能点）
- 不改动：

## 输出格式
- 代码直接落在本目录，附带必要的测试
- 完成后在 README 末尾或 CHANGELOG 简述改动

## 约束
- 保持现有代码风格，最小改动
- 不引入新依赖，除非先说明理由
- 验收标准：（什么行为算完成）
`,
  },
  {
    label: "内容撰写",
    content: `# 内容撰写

## 目标
为「」写一篇（公众号 / 博客 / 文档），读者是……

## 范围
- 必须覆盖的要点：
- 参考素材：references/ 下的……

## 输出格式
- 产出：draft.md
- 篇幅：约 字
- 结构：标题 → 开头钩子 → 正文小节 → 结尾行动建议

## 约束
- 口吻：（口语 / 专业 / 克制）
- 不用空泛形容词，观点要有依据或例子
`,
  },
];
